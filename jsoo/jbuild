;; -*- scheme -*-

(jbuild_version 1)

(executables
 ((names (main))
  (preprocess (pps (js_of_ocaml-ppx ppx_fields_conv)))
  (js_of_ocaml ((flags (+weak.js))))
  (libraries
   (base
    js_of_ocaml))))
